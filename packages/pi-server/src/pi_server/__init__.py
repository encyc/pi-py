"""pi-server: Python port of @earendil-works/pi-server.

实验性 agent 服务化层：supervisor、RPC 进程、IPC（Unix socket + JSONL）。

对应上游 ``packages/server``（TypeScript）。
"""

__version__ = "0.81.1"
__upstream_ref__ = "earendil-works/pi@v0.81.1"
